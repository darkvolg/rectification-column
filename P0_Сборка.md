# P0 — Закупка и сборка «прибора-наблюдателя»

Этап P0 из `ACTION_PLAN_Автоматика.md`. Мерит и пишет, **ничем не управляет**.
Конфиг прошивки: `esphome/kolonna_p0.yaml`

---

## Список закупки

Прямые ссылки не привожу — карточки на Ozon/Ali живут пару месяцев и цены пляшут.
Вместо этого точная модель, поисковая строка и **на что смотреть**, чтобы не купить не то.

### 1. ESP32 DevKit v1 — ~500 ₽

Поиск: `ESP32 DevKit v1 30 pin CH340`

- Брать **30-пиновую** версию. У 38-пиновой другая распиновка, схема ниже не сойдётся
- Мост USB: CH340 (драйвер ставится сам) или CP2102 — оба нормально
- Взять **две штуки**. Вторая на стол для экспериментов, чтобы не разбирать боевую

### 2. PZEM-004T v3.0 — ~900 ₽ ⚠️ главный пункт

Поиск: `PZEM-004T v3.0 100A трансформатор тока TTL`

- **Обязательно v3.0**, не v1/v2. У старых другой протокол, конфиг не подойдёт
- **Обязательно версия 100 А с трансформатором тока (разъёмное кольцо)**.
  Версия «10 A со шунтом» **не годится**: твой ТЭН 3 кВт = 13.6 А, шунт сгорит
- В комплекте должно быть кольцо CT. Если на фото только плата — не та версия
- На плате должен быть 4-пиновый разъём TTL (5V, RX, TX, GND)

### 3. DS18B20 в гильзе ×1 — ~150 ₽

**Три из четырёх уже стоят на колонне:** 2/3 высоты царги, верхняя точка отбора,
вода охлаждения. Их только подключить к ESP32.

**Не хватает одного — в кубе.** Без него нет автостопа по 98.5 °C, то есть нет
признака «тело кончилось». Это единственный датчик, который надо купить.

**Бобышка в кубе есть** — сверлить и паять медь не придётся, датчик просто
вкручивается или вставляется.

⚠️ **Перед заказом замерь бобышку.** От этого зависит, какой датчик брать:

- **Резьба G1/4"** — самый частый размер. Ищи `DS18B20 датчик с резьбой 1/4`
- **Резьба M10×1 или M12×1.5** — тоже встречается, есть готовые гильзы
- **Гладкое отверстие 6 мм** — брать обычную герметичную гильзу Ø6 мм,
  сажать на термопасту и фиксировать хомутом или прижимной гайкой

Если резьба нестандартная — берётся гладкая гильза Ø6 мм плюс переходник
или обжимной фитинг под 6 мм. Работать будет одинаково, вопрос только в герметичности.

Взять 2 штуки — вторая в запас.

<details>
<summary>Требования к датчику (если будешь докупать)</summary>

Поиск: `DS18B20 герметичный датчик нержавейка кабель 2м`

- Брать **готовые в гильзе из нержавейки на кабеле**, а не голые TO-92.
  Гильза сажается в куб/царгу, голый чип туда не поставить
- Кабель 2 м — хватит до корпуса. Длиннее 5 м 1-Wire начинает капризничать
- **Про подделки честно:** дешёвые DS18B20 с Ali часто клоны с разбросом до ±2 °C.
  Лечится сравнением: перед установкой опусти все три в стакан с водой комнатной
  температуры, запиши расхождение, вбей поправку в конфиг (`filters: offset`).
  Тебе нужен **тренд**, поэтому клоны сгодятся, но поправку сними обязательно

</details>

**Про уже стоящие датчики:** сверку в стакане с ними не сделать, они вмонтированы.
Поправку снимешь иначе — на холодной колонне перед разгоном все четыре должны
показывать одну комнатную температуру. Расхождение и есть поправка, вбивается
в `filters: - offset:`.

### 4. BMP280 — ~200 ₽

Поиск: `BMP280 модуль барометр I2C 3.3V`

- Не путать с BME280 (тот дороже, умеет ещё влажность — она не нужна)
- Убедиться, что модуль **I2C** (4 пина: VCC GND SCL SDA), а не SPI-версия на 6 пинов

### 5. Счётчик капель — ~300 ₽ · НЕ ОБЯЗАТЕЛЕН

Сначала главное, выясненное по серийным контроллерам (SSVC0059 «Смартмодуль»,
ШИМ-2, БКУ-07): **измерять расход отбора вообще не нужно для управления.**
Скорость задаётся разомкнутым ШИМ клапана, а обратная связь идёт по температуре
в царге. Расходомера в этих приборах нет ни в каком виде.

Значит счётчик — **контрольный прибор**, а не элемент управления. Ставится, чтобы
видеть график и убедиться, что ШИМ выдаёт то, что задумано. Полезно, но без него
автоматика работать будет.

**Сравнение вариантов, если всё же хочешь мерить:**

| Способ | ₽ | Оперативность | Минусы |
|---|---|---|---|
| **Счётчик капель** | 300 | секунды | объём капли плавает, на теле поток к границе |
| Весы HX711 | 550 | **десятки минут** | датчик 5 кг слишком груб для скорости |
| Опрокидывающийся ковш | 800 | ~минута | дискрет 8 мл, пластик в контакте со спиртом |
| Ничего | 0 | — | летишь по приборам ШИМ и температуры |

Про весы это не моя догадка: на профильном форуме разбор готового решения
«весы винокура» кончился выводом, что с датчиками 5–20 кг они дают нормальный
**суммарный объём**, но оперативную скорость не измеряют — только интегрально
на масштабе десятков минут. Ровно то ограничение, о котором я предупреждал раньше.

Ковш — это метеорологический датчик дождя (Oregon PCR800 и клоны): лоток
опрокидывается каждые 8 мл, геркон даёт импульс. Готовое решение, но дискретность
грубая и спирт контактирует с пластиком лотка.

**Вывод: счётчик капель — лучший из измерителей, но бери его как приборчик
для графика, а не как основу управления.**

Ниже — как собрать, если решил ставить.

Поиск: `щелевой оптопрерыватель модуль LM393` (он же «датчик скорости энкодера»,
чёрная П-образная оптопара на платке) — ~150 ₽
Плюс: **система для внутривенных вливаний** из аптеки — ~40 ₽

- У медицинской капельницы есть прозрачная камера, сделанная ровно для того, чтобы
  считать капли. Врезается в разрыв шланга отбора, капля падает в воздухе
- Щелевая оптопара надевается на эту камеру снаружи: ИК-светодиод с одной стороны,
  фототранзистор с другой. Капля пересекает луч — импульс
- Брать модуль **с компаратором LM393** (три или четыре пина: VCC, GND, DO, иногда AO).
  Тогда на ESP32 приходит готовый цифровой фронт, ничего паять не надо
- Ширина щели должна быть больше диаметра камеры. Если не налезает — разломать модуль
  и разнести светодиод с фототранзистором по бокам камеры на проводах
- **Камеру закрыть от света** — обмотать чёрной изолентой, оставив окошки под оптопару.
  Солнце из окна собьёт счёт

**Про точность честно:** объём капли не константа, плавает от 0.03 до 0.05 мл в
зависимости от температуры и скорости. Абсолютной точности здесь нет. Но регулятору
она и не нужна — нужна повторяемость, а её счётчик даёт отлично. Калибруется один раз:
накапал 100 мл в мерный цилиндр, поделил на число капель, вбил мл/каплю в конфиг.

**Где перестанет работать:** если поток превратится в струйку, счёт занизит.
На голове 120 мл/ч это примерно 1 капля в секунду — вообще не проблема.
На теле 1100 мл/ч это уже 7–8 капель в секунду, ближе к границе. Если увидишь,
что на теле счёт врёт, — тело всё равно ведётся по температуре, а не по объёму.

<details>
<summary>Вариант Б: весы (если счётчик капель не пойдёт)</summary>

HX711 + балочный тензодатчик **5 кг** (на теле в банке 4.4 кг), ~550 ₽. Точнее по
абсолютному объёму, но требует ровной площадки, тары, жёсткого крепления шланга
и закрытой банки. Шум ±1–2 г против нужных 8.1 г за 5 минут — работает, но впритык.
Подключение: DOUT GPIO18, SCK GPIO19, питание строго 3.3 В.

</details>

### 6. Питание — ~300 ₽

Поиск: `блок питания 5V 2A DC 5.5x2.1`

- **Отдельный БП.** Не запитывать ESP32 от чего-либо, связанного с ТЭНом
- Гнездо DC под пайку на плату

### 7. Мелочёвка — ~400 ₽

- Резистор **4.7 кОм** (подтяжка 1-Wire) — 1 шт, взять десяток
- Резисторы **1 кОм и 2 кОм** (делитель на RX от PZEM) — по 2 шт
- Клеммники винтовые 2- и 3-контактные
- Корпус пластиковый, **150×110×70 мм**, IP54 или IP65 — расчёт размера и
  почему не металлический, в разделе «Какой корпус брать» ниже
- Провод МГТФ или любой многожильный тонкий
- Термоусадка

**Итого ~3300 ₽.**

---

## Схема подключения

### Распиновка ESP32

| GPIO | Куда | Примечание |
|---|---|---|
| **GPIO4** | DS18B20 DATA (все параллельно) | + резистор 4.7 кОм на 3.3 В |
| **GPIO16** | PZEM TX → сюда | **через делитель 1к/2к**, см. ниже |
| **GPIO17** | PZEM RX ← отсюда | напрямую |
| **GPIO21** | BMP280 SDA | |
| **GPIO22** | BMP280 SCL | |
| **GPIO18** | Счётчик капель, выход DO | модуль 2, пока свободен |
| **GPIO19** | YF-S201C, жёлтый сигнальный | **через делитель 10к/20к**, см. ниже |
| **GPIO23** | Сирена, затвор полевика | через 100 Ом, + 10 кОм затвор→GND |
| **GPIO13** | microSD MOSI | |
| **GPIO14** | microSD SCK | |
| **GPIO27** | microSD MISO | |
| **GPIO26** | microSD CS | перенесён с GPIO5, см. ниже |
| 3V3 | питание DS18B20, BMP280 | |
| VIN | +5 В от БП | сюда же питание YF-S201C |
| GND | общий минус | |

Пины 6–11 не трогать (там флеш). 0, 2, 12, 15 не занимать — мешают загрузке.

### Датчики назначаются из интерфейса, а не прошивкой

Роль датчика (куб, царга, отбор, вода) **не зашита в прошивку**. Прошивка
видит только то, что физически нашла на шине — это **слоты**. Какой слот
чему соответствует, задаётся в интерфейсе и хранится в самой плате, поэтому
переживает перезагрузку и одинаково для всех, кто откроет пульт.

**Зачем так.** Сегодня датчик в кубе, завтра он же в царге, послезавтра
сгорел и куплен новый с другим адресом. Если роль прибита к адресу в
прошивке, каждая такая мелочь требует перепрошивки с компьютера. Прибор,
который планируется отдавать другим людям, так работать не может.

**Как назначить:**

1. Настройки → «Какой датчик где стоит».
2. Зажать датчик в кулаке и смотреть таблицу слотов — который поехал вверх,
   тот и в руке.
3. Выбрать этот слот в нужной строке. Всё, перепрошивка не нужна.

Роль «нет» означает, что датчик не подключён: плашка на пульте пустая, и
аварии по ней молчат. Это сделано намеренно — честное «нет данных» лучше
чужого показания.

**Ограничение, о котором надо знать.** Слоты нумеруются по возрастанию
адресов датчиков. Добавил или заменил датчик — порядок может сдвинуться, и
роли уедут. Видно это сразу: показания встанут не в свои плашки. Лечится
переназначением в интерфейсе за десять секунд.

### Ошибка, которая была до этого (07.09.2026)

У всех пяти датчиков в прошивке стоял адрес-заглушка `0x0000000000000000`.
ESPHome в этом случае подставляет каждому сенсору первый найденный на шине
датчик. При одном подключённом датчике все пять каналов показывали его
копию, а индикатор бодро сообщал «Датчики 4 из 4».

Ловится по тому, что значения совпадают до последнего разряда.

**Так гнать нельзя:** автостоп по кубу и авария охлаждения сработали бы от
датчика, который не в кубе и не на воде. Отсюда и переход на слоты.

### Диагностика через логи платы

Если надо посмотреть, что физически на шине:

1. Временно поставить `logger: level: DEBUG`. На `INFO` адреса не печатаются.
2. Собрать и залить: **`esphome run`**, не `esphome upload`.
   `upload` берёт уже собранный бинарник, и правки в него не попадают —
   в логах при этом видно старую дату сборки.
   ```powershell
   $env:ESPHOME_DATA_DIR = "C:/esphome_data"
   esphome run "C:/Users/Dark/Desktop/Проекты/Водочка/esphome/kolonna_p0.yaml" --device 192.168.0.55 --no-logs
   ```
3. Запустить `esphome logs ... --device 192.168.0.55`, дать подключиться и
   перезагрузить плату. Адреса печатаются только при загрузке:
   ```
   [C][gpio.one_wire:087]:   Found devices:
   [C][gpio.one_wire:090]:     0x3b0000005209c928 (DS18B20)
   ```

Первый снятый датчик: `0x3b0000005209c928` (07.09.2026).

### Барометр оказался BMP180, а не BMP280

В списке закупки значился BMP280, физически стоит **BMP180**. Отличия,
которые важны при сборке:

- адрес жёстко `0x77`, менять нельзя (у BMP280 бывает 0x76 и 0x77);
- в ESPHome другой драйвер: `platform: bmp085` (BMP085 и BMP180 — один чип
  с точки зрения драйвера);
- точность ±1 гПа против ±0.12 у BMP280. Для поправки температуры кипения
  это несущественно: 1 гПа даёт 0.028 °C.

Симптом неверного драйвера: `Wrong chip ID or no response` при том, что
i2c-скан устройство на шине видит.

### Какой вывод в каком ряду — от этого зависит вся раскладка платы

Положи плату разъёмом USB **влево**, шелкографией вверх. Тогда:

```
верхний ряд:  VIN  GND  D13 D12 D14 D27 D26 D25 D33 D32 D35 D34  VN  VP  EN
              [USB]
нижний  ряд:  3V3  GND  D15 D2  D4  RX2 TX2 D5  D18 D19 D21 RX0 TX0 D22 D23
```

`RX2` = GPIO16, `TX2` = GPIO17, `VN` = GPIO39, `VP` = GPIO36.

- **Нижний ряд — вся периферия колонны:** D4 (1-Wire), RX2 и TX2 (PZEM),
  D19 (проток), D21 и D22 (BMP280), D23 (сирена), плюс 3V3. Поэтому на плате
  клеммники стоят **внизу**, в том же порядке, что и эти выводы, а шина +5 В
  идёт по нижнему краю.
- **Верхний ряд — питание и карта:** VIN, GND и D13/D14/D27/D26. Поэтому
  модуль microSD ставится **над** ESP32 — четыре провода идут прямо вверх,
  а +5 В поднимается к VIN одной жилой по левому краю.

**CS карты перенесён с GPIO5 на GPIO26.** GPIO5 сидит в нижнем ряду, и тянуть
его к карте пришлось бы вокруг всей платы. GPIO26 стоит рядом с остальными
линиями SPI. Дополнительный плюс: GPIO5 — strapping-вывод, при старте он
должен быть высоким, а карта может его придержать.

---

### Датчик протока YF-S201C

Турбинка с датчиком Холла. Резьба G1/2 наружная с обеих сторон, диапазон
1–30 л/мин, до 0.8 МПа, до 80 °C.

**Ставить на ХОЛОДНЫЙ ВХОД, до дефлегматора.** Не на выход, хотя интуиция
тянет туда:

- корпус пластиковый, горячая вода его старит;
- на выходе вода нагрета и часто идёт с пузырями — турбинка считает мусор;
- на входе давление водопровода стабильное, счёт ровный.

Стрелка на корпусе — по направлению потока. Ставить **горизонтально**,
осью крыльчатки вверх: так в неё не набивается воздух и меньше износ оси.
Перед датчиком желательно сетчатый фильтр — окалина из труб клинит крыльчатку.

Если дефлегматор и холодильник соединены последовательно (обычный вариант),
одного датчика на входе хватает на всё. Если параллельно — считает только
свою ветку, и тепловой баланс будет занижен.

#### Подключение — обязательно через делитель

Выход YF-S201C — прямоугольник амплитудой с питание. Питаем от 5 В, значит
на сигнальном проводе 5 В, а ESP32 к 5 В **не толерантен**.

```
  +5V (VIN) ────────────── красный

  GND ──────────────────── чёрный

  жёлтый ──[10 кОм]──┬──── ESP32 GPIO19
                     │
                 [20 кОм]
                     │
                    GND
```

5 В × 20 / (10 + 20) = **3.33 В** — ровно то, что нужно.

**Если импульсов нет вообще** (в логах Water Flow всё время 0, хотя вода идёт):
у твоего экземпляра выход с открытым коллектором без внутренней подтяжки.
Лечится одним резистором — **10 кОм с жёлтого на +5 В**, делитель остаётся
на месте.

Проверка на столе, без водопровода: подуть в датчик и смотреть логи.
Крыльчатка раскручивается от дыхания, импульсы пойдут.

#### Калибровка — обязательна

Паспортные 450 импульсов на литр — среднее по модели, разброс между
экземплярами до 10%. От этой цифры напрямую зависит расчётная мощность
по воде, так что калибровать надо.

1. Нажать в HA **«Reset Water»**
2. Пролить в ведро **не меньше 5 литров** (чем больше, тем точнее)
3. Взвесить набранное на кухонных весах: 1 кг = 1 л
4. Посмотреть `Water Total` — он покажет литры по текущему коэффициенту
5. Новый коэффициент: `450 × (показания Water Total) / (реальные литры)`
6. Вписать в `substitutions: pulses_per_liter`, перезалить

Калибровать **на рабочем расходе**, а не на полностью открытом кране:
турбинка нелинейна на краях диапазона, и коэффициент, снятый при 10 л/мин,
на рабочих 1.5 л/мин соврёт.

#### Что это даёт, кроме аварии

Вторая, независимая мощность — по тепловому балансу:

```
P [Вт] = расход [л/мин] × (T_выход − T_вход) [°C] × 69.77
```

Почти всё тепло ТЭНа уносит охлаждающая вода. При 900 Вт и расходе
1.5 л/мин разность температур воды выйдет около 8.6 °C.

Ценность в том, что это **не зависит от PZEM**. Две мощности, посчитанные
разными способами, сходятся в пределах 10–15% → обеим можно верить.
Разошлись — врёт либо сопротивление ТЭНа, либо калибровка импульсов.

Показания по воде на 5–10% ниже электрических — это норма, часть тепла
уходит через изоляцию колонны.

### Заодно: что делать со старой коробкой

В белой коробке на царге сейчас Arduino с Bluetooth-модулем, к нему заведены датчики.
ESP32 заменяет эту связку целиком: те же DS18B20, но по WiFi и сразу в Home Assistant,
без телефона и без ручного снятия показаний.

Провода датчиков просто перецепить с Arduino на ESP32 — 1-Wire есть 1-Wire. Сам корпус
можно переиспользовать, если влезет ESP32 с обвязкой. **PZEM в этот корпус не ставить** —
он на 220 В, ему отдельная коробка подальше от колонны.

---

## Монтаж платы (перфоплата 70×100 в корпусе 150×110×70)

Не печатная плата — перфоплата (точечная, монтаж навесным проводом) ради
экономии: не нужно ждать и заказывать PCB. Полная схема размещения и
разводки — [P0_Монтажная_плата.svg](P0_Монтажная_плата.svg), открыть
в браузере.

На плате: ESP32 (на гнездовых колодках, не впаян намертво — вынимается для
перепрошивки), делители YF-S201C и PZEM RX, подтяжка 1-Wire, ключ сирены
(Q1 + резисторы), BMP280, модуль microSD. Сам PZEM и YF-S201C остаются
снаружи — заходят на плату четырьмя и тремя проводами через клеммники.

### Какой корпус брать

Перфоплата 70×100 мм — это пол. Вокруг неё нужен запас на стойки, на радиус
изгиба проводов у клеммников и на гермовводы, которые заходят сбоку.

**Что стоит на плате и сколько занимает в высоту:**

| Что | Пятно, мм | Высота, мм |
|---|---|---|
| ESP32 DevKit 30 pin на гнездовых колодках | 55×28 | **~20** (колодки + плата + USB сверху) |
| Модуль microSD (SPI) | 42×24 | ~10 с картой |
| BMP180 / BMP280 | 15×12 | ~5 |
| Клеммники винтовые, 5 шт по краям | вдоль борта | ~10 |
| Зуммер 5 В | Ø12–23 | ~10 |

Самый высокий — ESP32 на колодках, около 20 мм. Плюс стойки под плату 4 мм,
сама плата 1.6 мм и зазор до крышки 5 мм: **внутренняя высота от 31 мм**.
По площади: 70×100 плюс по 8 мм на сторону — **внутри от 86×116 мм**.

Корпуса продают по наружному размеру, стенки съедают 2–3 мм с каждой стороны.

| Размер, мм | Годится |
|---|---|
| 120×80×50 | Впритык. 80 наружных — это ~74 внутри при плате 70. Ставить можно, работать неудобно |
| **150×110×70** | **Брать этот.** Свободно ложится плата, остаётся место на вводы и на будущее |
| 200×120×75 | Тоже хорошо, если не жалко места на стене |

Класс защиты **IP54 достаточно**, IP65 не помешает: рядом кипит спирт и бывает
пар, но заливать корпус никто не собирается.

**Пять гермовводов PG7** (под кабель Ø3–6.5 мм): датчики 1-Wire, расходомер,
TTL от PZEM, сирена, питание. Либо четыре ввода плюс панельное гнездо DC 5.5×2.1.

### Четыре вещи, которые портят жизнь потом

**Корпус только пластиковый.** У ESP32 антенна на самой плате. Металлическая
коробка — это экран, связь исчезнет. По той же причине антенным краем платы
не упираться в стену корпуса, оставить 10 мм.

**PZEM в этот корпус не ставить.** Он на 220 В, ему отдельная коробка
(сам ~79×43×22 мм плюс кольцо трансформатора тока), подальше от колонны и
от пара. К плате от него идут четыре тонких провода TTL.

**Зуммер из запечатанной коробки почти не слышно.** Смысл сирены в том, чтобы
её услышали. Либо вынести зуммер наружу отдельным проводом, либо просверлить
под ним решётку в крышке и заклеить изнутри мембраной от влаги.

**Карту вынимают руками.** Модуль microSD ставится над ESP32 и упирается в
стенку. Либо клади его к краю и режь щель в корпусе, либо сразу соглашайся,
что за картой придётся открывать крышку. Второе нормально: журнал и так
целиком забирается по сети со страницы «Погоны».

### Что докупить сверх уже купленного

- Перфоплата точечная **~70×100 мм** — 60–80 ₽
- Винтовые клеммники: 2-контактные ×2 (питание, сирена), 3-контактные ×2
  (1-Wire, YF-S201C), 4-контактный ×1 (PZEM TTL) — по 10–15 ₽ за штуку
- Резистор **20 кОм** (вторая половина делителя YF-S201C) — в «Мелочёвке»
  выше указана только пара 1к/2к для PZEM, эта нужна отдельно
- Гнездовые колодки (пины) под ESP32, если не напаяны производителем

Резисторы 4.7 кОм, 1 кОм, 2 кОм и клеммники — уже в списке «Мелочёвка» выше.
Резистор 100 Ом и вторая подтяжка 10 кОм (затвор Q1) — берутся из того же
десятка 10 кОм, что и делитель YF-S201C.

### Раскладка: шины по краям, сигналы в середине

Главное правило перфоплаты — голых шин должно быть мало и лежать они должны
вплотную к своим потребителям. Иначе каждый сигнальный провод пересекает их
все, и монтаж превращается в кашу.

| Шина | Где лежит | Почему там |
|---|---|---|
| **+5 В** | голой жилой НАД клеммниками | три потребителя из четырёх (проток, сирена, PZEM) сидят прямо под ней — до каждого 2 см. Вниз к плате уходит одна жила на VIN |
| **GND** | голой жилой ПОД делителями | там нижние выводы R3/R5/R7 и исток Q1 — им до шины сантиметр. Верхние клеммники падают на неё напрямую |
| **+3.3 В** | одной линией с вывода 3V3 самой платы | потребителей всего три: подтяжка R1, BMP280, microSD |

Питание модулей BMP280 и microSD заведено по правому краю платы, а не через
середину — иначе оно резало бы шлейф SPI.

Голых шин две, всё остальное — изолированный МГТФ. Поэтому перехлёсты проводов
безопасны: на чертеже **точка означает пайку, перекрестье без точки — просто
провод поверх провода**.

### Порядок пайки

1. Две голые шины: +5 В над клеммниками, GND под делителями
2. Клеммники
3. Резисторы и Q1 — каждый рядом со своим клеммником
4. Гнездовые колодки под ESP32 **без вставленной платы** — паять рядом с уже
   воткнутой платой неудобно и легко перегреть её пластик
5. BMP280 и microSD
6. Перемычки МГТФ
7. ESP32 в колодки — последним

**Перед первым включением:**

- прозвонить мультиметром +5 В — GND на короткое замыкание, пока плата ещё не
  вставлена и USB не подключён;
- проверить делители тестером: подать 5 В на вход и убедиться, что на выводах
  под GPIO19 и GPIO16 ровно 3.3 В, а не 5 В. Иначе выгорит вход ESP32.

---

## 🔴 Куда подключать PZEM — прочитать до врезки

Два правила, каждое проверено чужой сгоревшей платой.

### Правило 1. Клеммы напряжения — только ДО диммера

```
  Розетка ──┬──► [клеммы U на PZEM]      ← ТОЛЬКО ЗДЕСЬ, чистая синусоида
            │
            └──► [ДИММЕР] ──► [кольцо CT] ──► ТЭН
```

Подключение клемм напряжения **после** симистора выжигает входную цепь модуля.
На форуме самогонщиков: «Я вот подключил и с r11 пошёл дымок». PZEM хочет
целую синусоиду, огрызки он не переваривает.

Кольцо CT — наоборот, **после** диммера, на проводе к ТЭНу. Оно меряет ток
и режимом ему всё равно.

### Правило 2. Полю «мощность» не верить

При таком подключении PZEM перемножает напряжение целой синусоиды на ток
рубленой. Мощность выходит завышенной, причём ошибка зависит от угла отсечки.

Поэтому рабочая мощность **вычисляется**, а не измеряется:

```
  ТЭН — чисто активная нагрузка, значит   P = I² · R
  и это верно ПРИ ЛЮБОЙ ФОРМЕ ТОКА.
```

Нужен только честный действующий ток (его PZEM меряет трансформатором честно)
и сопротивление ТЭНа. Паспортное R = 220² / 3000 = 16.13 Ом, уточняется
калибровкой ниже. В конфиге это сенсор `Power`, а сырое поле PZEM оставлено
рядом как `Power Raw` — по расхождению между ними видно, насколько порезана
синусоида.

---

## ⚠️ Безопасность на этапе P0

1. **TTL-сторона PZEM-004T опторазвязана** — питание +5 В идёт на цепь после
   оптронов. Это подтверждается тем, что без подключения к сети модуль вообще
   не отвечает по UART: питается он от измеряемого напряжения.
2. **Но правило про USB всё равно соблюдать:** программировать ESP32 только
   при полностью отключённой от сети колонне. Развязка есть, но это дешёвый
   китайский модуль, оптрон пробивается, а цена ошибки — удар током и сожжённый
   ноутбук. Отключил колонну → воткнул USB → прошил → вынул USB → включил.
3. Собранный PZEM — только внутри закрытого корпуса, отдельного от коробки
   с датчиками. Ничего не торчит наружу.
4. Кольцо CT защёлкивать на проводе **при снятом напряжении**.
5. УЗО 30 мА на розетку колонны — ставить уже сейчас, не откладывая до P3.
6. **Стенд без сети не заработает.** PZEM питается от измеряемого напряжения,
   +5 В на TTL недостаточно — на столе он будет молчать. Проверять его только
   после врезки в сеть.

---

## Порядок сборки

Слаботочка — первая. К сети подходим только на шаге 9, когда всё остальное
уже проверено и работает.

**Стол, сеть выключена:**

- [ ] **1.** Собрать всё на столе: ESP32, пять DS18B20, BMP280, зуммер, YF-S201C.
  Питание от БП 5 В. PZEM пока в стороне
- [ ] **2.** Поставить ESPHome (аддон в HA 192.168.0.74) или ESPHome CLI
- [ ] **3.** Залить `esphome/kolonna_p0.yaml`, вписав свои WiFi и ключи
- [ ] **4.** Открыть логи. Найти строки вида:
  ```
  [dallas_temp] Found sensor 0x1c0000031edd2a28
  ```
  Выписать **все адреса** и вставить в конфиг вместо заглушек `0x0000...`.
  Какой где — определять по поведению: зажать датчик в кулаке и смотреть,
  чей график поехал вверх
- [ ] **5.** Все датчики в один стакан воды. Обязаны показать одно и то же.
  Расхождение вбить поправками через `filters: - offset:`
- [ ] **6.** Проверить зуммер: включить `Siren` из HA. Не запищал — транзистор
  не открылся (см. примечание про IRFZ44N ниже)
- [ ] **7.** Проверить проток: подуть в YF-S201C, в логах должен дёрнуться
  `Water Flow`. Ноль — смотреть раздел «Если импульсов нет вообще»

**Колонна, сеть выключена:**

- [ ] **8.** Вкрутить T1 в бобышку куба. Три средних уже стоят — перецепить
  с ардуинки на GPIO4. T5 хомутом на вход воды
- [ ] **9.** Врезать YF-S201C в холодный вход, стрелкой по потоку, горизонтально.
  Пустить воду, проверить на течь **до** того, как рядом появится 220 В
- [ ] **10.** Обесточить колонну, врезать PZEM: клеммы напряжения **до диммера**,
  кольцо CT на фазе после диммера. См. «Куда подключать PZEM» выше

**Сеть включена, USB отключён:**

- [ ] **11.** **Отключить USB.** Подать сеть. Убедиться, что ток и напряжение видны
- [ ] **12.** **Калибровка R.** Диммер на максимум (синусоида целая, PZEM меряет верно),
  записать `Power Raw` и `Current`, посчитать `R = P / I²`, вписать в
  `ten_resistance`. Ожидается около 16 Ом
- [ ] **13.** **Калибровка протока** на рабочем расходе — процедура выше
- [ ] **14.** Записать реальную мощность при твоём обычном положении крутилки ← ради этого всё
- [ ] **15.** Сверить `Power` и `Power Water`. Разница больше 15% — разбираться,
  какая из двух калибровок врёт
- [ ] **16.** Отогнать партию как обычно, вручную. Смотреть логи
- [ ] **17.** Разобрать график: когда реально кончились головы, сколько отобрано

### ⚠️ Про транзистор IRFZ44N

Куплен IRFZ44N вместо IRLZ44N. Буква **L** значит logic level — порог затвора
1–2 В. У IRFZ порог до 4 В, а ESP32 даёт 3.3 В.

Для зуммера на 20–30 мА этого хватает: ток мизерный, приоткрытого канала
достаточно. Но у части экземпляров порог как раз 4.0 В, и такой не откроется.

Проверить обе штуки на столе (шаг 6). Если оба молчат — поставить обычный
NPN, **BC337** или **2N2222**: база через 1 кОм на GPIO23, эмиттер на GND,
коллектор на минус зуммера. С 3.3 В открывается гарантированно.

---

## Что увидишь в Home Assistant

| Сущность | Смысл |
|---|---|
| **Power, Вт** | **правда вместо вранья вольтметра.** Считается как I²·R |
| **Power Water, Вт** | вторая мощность, по теплу воды. Независимая проверка первой |
| Power Raw | сырое поле PZEM. Расхождение = мера порезанности синусоиды |
| Напряжение / ток | контроль сети |
| T1 куб | автостоп-ориентир 98.5 °C |
| T2 2/3 царги | фронт примесей приходит сюда первым |
| T3 отбор | главный рабочий, по нему видно приход тела |
| T4 вода выход | рост = охлаждение слабеет |
| T5 вода вход | нужен только для Power Water |
| **Delta T** | T3 − T2. Не зависит от погоды, обе точки плывут вместе |
| **Trend T2** | °C/мин. Ловит фронт за 20–30 мин до сдвига абсолютной температуры |
| **Water Flow, л/мин** | авария по протоку срабатывает раньше температурной |
| Water Total, л | расход воды за партию. Вода + водоотведение 70 руб/м³ |
| Атм. давление + поправка Т кипения | чтобы смена погоды не выглядела как хвосты |

Плюс аварии: `ALARM Cooling`, `ALARM No Flow`, `ALARM Kub Hot`,
`ALARM Sensor Fault`, `WARN Power`, `WARN Front`. Любая из них включает сирену
локально, решением самого ESP32 — роутер и Home Assistant для этого не нужны.

Капли и скорость отбора появятся в модуле 2, счётчик пока не подключён.

---

## Дальше

После партии с логами — садимся за график и настраиваем P1 (автоматический отбор)
уже по фактическим цифрам, а не по расчётным.
