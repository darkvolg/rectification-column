# Автоматика колонны — план по модулям

Каждый модуль работает сам по себе. Можно остановиться в любой точке
и ничего не сломается.

> Ставишь эту сборку себе? Первым делом — [УСТАНОВКА.md](УСТАНОВКА.md).
> Ключи и пароли каждый генерирует свои, готовую прошивку мы не раздаём.

---

## Что уже есть

Arduino + Bluetooth, программа на компе, ручной журнал раз в час.
Датчики: **куб, 2/3 царги, точка отбора, вода охлаждения** — все четыре.
Плюс барометр и **счётчик расхода воды YF-S201C**. Всё это переезжает
как есть, покупать не надо.

**Болячки, которые лечим:** Bluetooth отваливается, звук на ноуте слабый,
журнал пишется руками, разрешение датчиков 0.25 °C — мелкий дрейф не виден,
вольтметр врёт вдвое.

---

# МОДУЛЬ 0 — тест на альдегиды ✅ куплен 01.09.2026

**307 ₽. Отвечает на единственный денежный вопрос проекта: где кончаются головы.**

`Индикатор голов самогона, тест на альдегиды, 30 мл`, артикул 3421330164, доставка 04.09.

Реактив Шиффа (фуксинсернистая кислота): бесцветный, альдегид возвращает окраску
розовый → фиолетовый. Ацетальдегид — главный компонент голов.

**Почему это не заменяется термометром.** Альдегид в головах — миллиграммы на литр,
на температуру кипения не влияет вообще. Царга во время голов стоит на месте:
кипит тот же спирт, просто грязный. Поэтому серийные контроллеры берут головы
по таймеру — мерить нечего. Температура работает только на хвостах, там состав
меняется на проценты.

## ⚠️ Чего тест не делает

На фото товара показаны пробирки «метанол / этанол». **Метанол этим флаконом
не определяется.** У метанола нет альдегидной группы. Настоящий тест требует
окислить его перманганатом до формальдегида, погасить избыток щавелевой кислотой
и только потом добавлять фуксинсернистую — три реактива и процедура.

Не использовать для решения «безопасно ли пить». Только «где кончились головы».

Также не видит: метанол, эфиры (этилацетат — вторая половина голов по массе),
сивуху в хвостах. «Прозрачный» значит только «ацетальдегида мало».

## Докупить, ~250 ₽

| Что | Зачем | ₽ |
|---|---|---|
| Дистиллированная вода 1 л | разбавление проб, аптека/автомагазин | 50 |
| Шприц 5 мл с делениями, 2 шт | отмерять пробу и воду | 30 |
| Пробирки или одинаковые рюмки, 12 шт | ряд проб за погон | 150 |
| Белый лист А4 | фон для чтения цвета | 0 |

## При получении

- **Дата розлива на флаконе** — реактив живёт месяцами, не годами
- Жидкость бесцветная или чуть желтоватая. **Пришёл розовым — брак, возврат 15 дней**
- Хранить в холодильнике, в темноте, крышка плотно

## Протокол — иначе показания несравнимы

Реактив чувствителен к крепости. Головы 92%, тело 60% дадут разный цвет
при одинаковом альдегиде. Поэтому **каждую пробу разбавлять до 40%.**

| Крепость пробы | На 1 мл пробы воды |
|---|---|
| 96% | 1.4 мл |
| 94% | 1.35 мл |
| 92% | 1.3 мл |
| 90% | 1.25 мл |

Практически: **1 мл пробы + 1.3 мл воды** покрывает весь диапазон голов и тела.
Пересчитывать только если крепость ушла ниже 85%.

Дальше одинаково для всех проб:
1. 1 мл реактива + одинаковое число капель разбавленной пробы (считать!)
2. Выдержка 10–15 мин, одна и та же
3. Читать на белом фоне при дневном свете
4. Контроль рядом — тело прошлой удачной партии

Сравнение проб **между собой** честное. Сравнение с картинкой на этикетке — примерное.
Глазом различаешь три состояния: чисто / есть / много. Не восемнадцать градаций
со шкалы.

## Первый погон с тестом

Процесс **не менять**. Гнать как обычно. Задача — снять кривую.

Проба каждые 30 минут на головах. Пробирку подписать: время, объём отобранного,
крепость. Выстроить в ряд, сфотографировать.

В журнал добавить три колонки:

| Время | Отобрано, мл | Крепость | Цвет пробы (1–5) |
|---|---|---|---|

Что смотреть: **на каком часу цвет перестал меняться.** Если на третьем,
а ты гонишь головы 8.5 — это ответ на весь проект за 307 ₽.

---

# МОДУЛЬ 1 — всё измерение сразу

**Цель:** убрать Bluetooth, повесить сирену, узнать реальную мощность,
получить дашборд и автологи.

После него ты видишь и записываешь **всё, что происходит в колонне**.
Управления пока нет — оно начинается с модуля 3.

## Состав, ~3150 ₽

Уже оплачено:

| Что | ₽ | Придёт |
|---|---|---|
| ✅ `PZEM-004T 3.0 100A Open CT` | 681 | 12–23 сент |
| ✅ `зуммер SFM-27 активный пьезо DC 3-24В`, 90 дБ | 130 | 3 сент |
| ✅ `IRLZ44N` ×3 | 239 | 8 сент |
| ⚠️ `IRFZ44N` ×2 — куплен по ошибке, см. ниже | 138 | 3 сент |
| ✅ Счётчик воды `YF-S201C` | есть | — |

Осталось добрать:

| Что | Поисковая строка | ₽ |
|---|---|---|
| ESP32 | `ESP32 DevKit v1 30 pin CH340` — **строго 30-пиновый** | 500 |
| Пятый DS18B20 | `DS18B20 герметичный датчик 1м` — на вход воды | 150 |
| Резисторы | 4.7к, 100, 10к, 1к, 2к, 20к — по десятку | 150 |
| Питание | `блок питания 12V 1A` + `модуль LM2596` понижающий | 450 |
| Корпус | `корпус IP54 120x80x50`, клеммники, провод, термоусадка | 400 |
| **Карта журнала** | `модуль microSD SPI Arduino` + карта 4–8 ГБ | **250** |

### Про карту

Контроллер пишет журнал погона сам, раз в 20 секунд, и отдаёт готовым CSV
для Excel по адресу `http://<ip>/log.csv`. Без карты журнал живёт
в оперативной памяти: 11 часов и до первой перезагрузки.

С картой — все погоны хранятся вечно, список открывается по `/logs`,
а саму карту можно вынуть и вставить в компьютер. 250 ₽ за то, чтобы
не потерять погон из-за случайного ребута.

Подключение: CS → GPIO5, SCK → GPIO14, MOSI → GPIO13, MISO → GPIO27.
Пины выбраны из свободных — 4, 16–19, 21–23 заняты датчиками и сиреной.

### ⚠️ Про транзисторы

Первым заказом ушёл **IRFZ44N** вместо **IRLZ44N**. Буква L значит logic level:
порог затвора 1–2 В против 4 В у обычного. ESP32 даёт 3.3 В.

Для зуммера на 20–30 мА IRFZ, скорее всего, откроется — ток мизерный.
Но у части экземпляров порог ровно 4.0 В, и такой не заработает. Поэтому
докуплены правильные IRLZ44N, придут 8 сентября.

IRFZ44N не выбрасывать: пойдут в силовые модули, где затвор питается от 12 В.

### 🔴 Куда ставить счётчик воды

**На холодный ВХОД, до дефлегматора.** Не на выход, хотя туда тянет интуиция:
корпус пластиковый и горячая вода его старит, на выходе вода идёт с пузырями
и турбинка считает мусор, а на входе давление стабильное.

Стрелка по потоку, ставить горизонтально осью вверх. Перед ним желательно
сетчатый фильтр — окалина клинит крыльчатку.

**Два места, где берут не то:**

- **PZEM** — обязательно **с трансформатором тока** (кольцо), а не «10 А со шунтом»:
  ТЭН даёт 13.6 А, шунт сгорит. Диапазон 32 А или 100 А — оба годятся, при 13.6 А
  работа в комфортной зоне точности.
  Протокол должен быть **Modbus-RTU** (версии 3.0 и новее) — старые v1/v2 говорили
  по своему протоколу, ESPHome их не понимает. Прозрачный синий корпус = современная.
  Если сомневаешься, спроси продавца в вопросах.
  Кольцо лучше **разъёмное** (split core, раскрывается защёлкой) — защёлкивается
  на готовом проводе. Глухое (closed) тоже работает, но провод надо продевать насквозь.
- **ESP32** — **30-пиновый**. У 38-пинового другая распиновка

Вторая плата ESP32 (+500 ₽) — по желанию: удобно настраивать на столе,
не разбирая боевую, и страховка на случай «спалил посреди сезона».

## Собрать — сначала слаботочку

Все пять датчиков — на **одну шину, GPIO4**, плюс резистор 4.7 кОм на 3.3 В.
Барометр — GPIO21/22. Сирена — GPIO23 через IRLZ44N.
Счётчик воды — GPIO19 **через делитель 10к/20к**: он отдаёт 5 В, а ESP32
к 5 В не толерантен.

```
GPIO23 ──[100 Ом]── затвор IRLZ44N
                    сток ── зуммер (−) ; зуммер (+) ── +12 В
                    исток ── GND
        [10 кОм] между затвором и GND — иначе взвоет на старте
```

Зуммер активный: подал питание — пищит сам, частоту гнать не надо.
Напрямую в GPIO не вешать, у него 20–30 мА — на грани для пина ESP32.
Полярность соблюдать: красный плюс, чёрный минус.
Питать от 12 В, а не от 5: активные зуммеры громче при большем напряжении.

PZEM пока не подключать: он всё равно молчит без сети, питается от измеряемого
напряжения. Схема и распиновка целиком — `P0_Сборка.md`.

## Прошить

Файл `esphome/kolonna_p0.yaml`.
1. Скопировать `secrets.yaml.example` в `secrets.yaml`, вписать WiFi
2. Залить, в логах найти четыре адреса DS18B20, вставить в конфиг
3. На холодной колонне сверить показания — все четыре должны дать одну
   комнатную температуру. Расхождение вписать в `offset`

## 🔴 Врезать PZEM — единственное опасное место

**Клеммы напряжения — только ДО диммера.** После симистора модуль выгорает,
проверено чужой сгоревшей платой. Кольцо CT — наоборот, после диммера,
на проводе к ТЭНу.

**Прошивать по USB только при выдернутой из розетки колонне.**

Кольцо CT защёлкивать при снятом напряжении. УЗО 30 мА на розетку колонны.

Подробно: `P0_Сборка.md`, раздел «Куда подключать PZEM».

## Откалибровать мощность

Мощность не измеряется, а **вычисляется**: `P = I² · R`. Так сделано потому,
что при фазовой отсечке PZEM перемножает напряжение целой синусоиды на ток
рубленой и завышает. А для чисто активного ТЭНа формула верна при любой форме тока.

Калибровка один раз: диммер на максимум (синусоида целая, PZEM верен),
записать `Power Raw` и `Current`, посчитать `R = P / I²`, вписать
в `ten_resistance`. Ожидается около 16 Ом.

## Дашборд и логи

`ha/dashboard_kolonna.yaml` — вставить копипастой в Home Assistant.
`web/index.html` — свой пульт, работает и без HA.
Подробности: `ha/README.md`.

**Логи пишутся сами**, каждые 10 секунд. В `configuration.yaml` поднять хранение:

```yaml
recorder:
  purge_keep_days: 180
  commit_interval: 5
```

Выгрузка в Excel: История → выбрать датчики и период → «Скачать данные» → CSV.
Либо настроить автоматизацию, которая сама дописывает строку раз в 30 минут —
тот же формат, что ты пишешь рукой.

## Откалибровать проток

Паспортные 450 импульсов на литр — среднее по модели, разброс до 10%.
Пролить 5 л в ведро, взвесить (1 кг = 1 л), сравнить с `Water Total`,
поправить `pulses_per_liter`. Калибровать **на рабочем расходе**, а не
на полностью открытом кране: турбинка нелинейна на краях диапазона.

## Что изменится

- Bluetooth больше нет
- Сирена на самом контроллере — ноут для звука не нужен
- Разрешение 0.0625 °C вместо 0.25 — виден мелкий дрейф
- **Известна реальная мощность** вместо 90 В на врущем вольтметре
- **Вторая мощность, по воде** — независимая проверка первой
- **Авария по протоку** срабатывает раньше температурной
- Журнал не пишешь руками

### Про две мощности

Почти всё тепло ТЭНа уносит охлаждающая вода, поэтому:

```
P [Вт] = проток [л/мин] × (T_выход − T_вход) [°C] × 69.77
```

Это считается **без PZEM**, из совсем других датчиков. Две цифры сошлись
в пределах 10–15% → обеим можно верить. Разошлись — врёт либо сопротивление
ТЭНа, либо калибровка импульсов на литр.

При 900 Вт и протоке 1.5 л/мин разность температур воды выйдет около 8.6 °C.
Показания по воде на 5–10% ниже электрических — норма, часть тепла уходит
через изоляцию.

### 🔴 Проток орёт, но не спасает

Датчик протока переехал сюда из модуля 3, но это **не значит, что колонну
уже можно оставлять одну**. Сейчас он только включает сирену. Уйти из дома
можно будет тогда, когда появится реле, которое по этой аварии физически
рвёт питание ТЭНа.

## Первый погон

Гнать **как обычно, руками**, ничем не управляя. Задача одна — набрать данные.

Что смотреть после:
- Какая мощность на самом деле при твоём обычном положении крутилки
- Тронулся ли тренд на 2/3 царги раньше, чем ты понял носом, что головы кончились

Эти два ответа определяют всё, что дальше.

---

# Дальше — по настроению

| Модуль | Что | ₽ | Что меняет |
|---|---|---|---|
| 2 | счётчик капель | 300 | скорость отбора без мерного стакана |
| 3 | реле в разрыв ТЭНа | 1200 | **можно уйти из дома** |
| 4 | клапан отбора, ШИМ | 2200 | автомат ведёт головы и тело |
| 5 | силовой узел | 1700 | держит мощность сам |
| 6 | автомат фаз | 0 | только код |

Модуль 3 идёт раньше 4 и 5 намеренно: симистор пробивается в замкнутое
состояние, и пока нет механического реле, автоматику включать нельзя.

Детали — в `ACTION_PLAN_Автоматика.md` (там они пронумерованы как P1–P4).

---

## Где деньги, если коротко

Вся экономия проекта — головы отбирались 8.5 ч вместо 3.3. Это 5 часов
и 0.6 л, около 3500 ₽ с партии.

**И чтобы её получить, автоматика не нужна.** Достаточно приоткрыть игольчатый
кран с 78 до 120 мл/ч: мерный стакан, секундомер, десять минут. Флегмовое число
останется 37, а чистота перестаёт расти уже после 20 — качество не пострадает.

Модули решают другое: удобство, надёжность и возможность отойти от колонны.
Это нормальные причины, просто не денежные.

---

## 🖥 Две разные вещи, специально разделённые

### Пульт колонны — `web/index.html`

Прибор. Открывать во время погона, показывает живые данные с ESP32.

| Страница | Что там |
|---|---|
| `web/index.html` | Аварии, значения, мнемосхема |
| `web/grafiki.html` | Четыре тренда, глубина 30 мин / 3 ч / 12 ч, выгрузка CSV |
| `web/nastroyki.html` | Адрес ESP32, оформление, экран, история |

История копится в браузере с шагом 30 секунд и переживает переход между
страницами и перезагрузку. Адрес можно зашить в ссылку и положить в закладки
телефона: `index.html?ip=192.168.0.55`.

### Документация — `web/doc/index.html`

Справочник. Открывать, когда паяешь или считаешь, а не когда гонишь.

| Страница | Что там |
|---|---|
| `web/doc/index.html` | Где я сейчас, что едет и с какими датами |
| `web/doc/plan.html` | Модули 0–6, лестница, решения «не пересматривать» |
| `web/doc/sborka.html` | Схемы, распиновка, калибровки, **чек-листы с галочками** |
| `web/doc/pogon.html` | Фазы, тест на альдегиды, **журнал партии с выгрузкой в Excel** |
| `web/doc/calc.html` | Мощность, флегма, захлёб, головы, ШИМ |

Галочки и журнал сохраняются в браузере. Чек-лист сборки можно распечатать
и унести к колонне. Интернет не нужен ни там, ни там.

MD-файлы остаются как исходники — правки удобнее вносить в них.

---

## Карта файлов

| Файл | Когда открывать |
|---|---|
| **`web/index.html`** | пульт колонны — во время погона |
| **`web/doc/index.html`** | документация страницами — когда паяешь или считаешь |
| **`НАЧНИ_ОТСЮДА.md`** | эта страница, текстом |
| `P0_Сборка.md` | схема, распиновка, калибровки |
| `esphome/kolonna_p0.yaml` | прошивка |
| `ha/README.md` | дашборд, логи, экспорт, MQTT |
| `ha/dashboard_kolonna.yaml` | дашборд для Home Assistant |
| `ACTION_PLAN_Автоматика.md` | модули 2–6, алгоритм отбора, силовой узел |
| `ЭКОНОМИКА.md` | экономика партии (не публикуется) |

Все схемы одной картинкой:
https://claude.ai/code/artifact/1fe42b73-44f3-42c1-a3e2-6f4b1f3d20dd
